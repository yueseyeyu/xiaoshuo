# run/ — 运行态文件

存放运行时生成的状态文件（不入 git，被 .gitignore `state.json` 规则覆盖）。

## 内容
- `state.json` — 系统运行状态快照（stage/章节进度/LLM 调用计数/债务/重试计数）
  - 原位于项目根目录，阶段 E 迁移至此

## 设计意图
分离"运行态"与"源码/配置"，避免 state.json 散落在根目录与其他文件混淆。
