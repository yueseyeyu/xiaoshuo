# 番茄小说 AI 辅助创作系统 —— v7.12 前端设计系统 v5

> 目标：从「通用管理后台」转向「专业创作者工作站」
> 视觉方向：克制深色、高信息密度、低卡片依赖、强内容层级
> 核心参考：Linear / Obsidian / 专业创作工具，减少 SaaS 仪表盘的卡片堆砌感

---

## 一、设计原则

1. **内容优先**：界面服务于小说创作与分析，装饰元素让位于信息本身。
2. **少卡片、多布局**：能用留白、分割线、列排版的区域，不用玻璃卡片包裹。
3. **一套主题变量，六套配色**：每套主题独立校准背景、强调色、文字、发光，避免简单色相旋转。
4. **实时反馈**：硬件、任务、设计建议等状态即时可见。
5. **AI 可介入骨架设计**：粗纲、细纲、世界观、角色、势力等设计模块明确提供 AI 优化建议与修改入口。

---

## 二、主题系统 v4

### 可见性要求

- 主题切换器常驻顶部导航栏右侧，以彩色圆点 + 主题名称形式呈现，不可收起。
- 当前主题圆点带高亮环，hover 显示完整名称。
- 主题选择持久化到 `localStorage`，刷新后保持。

### 六套配色

| 主题 | 主强调 | 次强调 | 背景 | 氛围 |
|------|--------|--------|------|------|
| `midnight` 午夜蓝（默认） | `#5B8DEF` | `#7AA7F5` | `#080C14` | 清透专业，默认生产力模式 |
| `forest` 森林 | `#22C55E` | `#4ADE80` | `#06130C` | 护眼深绿，长时间写作 |
| `ember` 烬 | `#F59E0B` | `#FBBF24` | `#140F0A` | 暖琥珀，夜间创作 |
| `nebula` 星云 | `#A78BFA` | `#C4B5FD` | `#0D0A14` | 柔和紫，灵感发散 |
| `ocean` 深海 | `#0EA5E9` | `#38BDF8` | `#070F16` | 冷静蓝，数据分析 |
| `obsidian` 曜石 | `#94A3B8` | `#CBD5E1` | `#050505` | 高对比灰，无障碍 |

### 语义色

| Token | 色值 | 用途 |
|-------|------|------|
| `--success` | `#22C55E` | 成功、运行中、健康 |
| `--warning` | `#F59E0B` | 警告、排队、需要注意 |
| `--danger` | `#EF4444` | 失败、高温、错误 |

> **主题色与语义色分离原则**：主题色（蓝/橙等）仅用于品牌元素（Logo、按钮、项目卡片背景、顶部装饰、进行中的管线节点）。状态/负载类指标（硬件仪表盘、管线完成态、健康条、统计卡片）强制使用语义色，不随主题切换而变化，避免用户误判。

| `--text` | 主题色 | 主文字 |
| `--text-secondary` | 主题色 65% | 次要文字、标签 |
| `--surface` | 主题色 | 卡片背景 |
| `--bg` | 主题色 | 页面背景 |
| `--border` | 主题色 12% | 边框、分割线 |

---

## 三、基础变量与间距

```css
--radius-sm: 6px;
--radius-md: 10px;
--radius-lg: 14px;
--radius-xl: 18px;
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 24px;
--space-6: 32px;
--topbar-height: 48px;
--hardware-bar-height: 24px;
```

---

## 四、布局系统

### 应用骨架

```
.app-shell
├── .topbar
├── .hardware-bar
├── .app-body
│   ├── aside.sidebar
│   └── main.main
│       └── section.page
```

- `.sidebar`：左侧导航，完整态 210px，icon-only 态 64px，移动端抽屉。
- `.main`：主内容区，padding 随页面类型调整（工作台较紧，写作较松）。

### 页面类型

1. **工作台 / 书库 / 拆书 / 报告 / 设计 / 设置 / 硬件**：标题 + 内容卡片，使用 `.page-header`。
2. **写作**：沉浸式编辑器，左侧内容、右侧辅助面板，专注模式隐藏所有 chrome。

---

## 五、组件规范

### KPI 卡片

- 使用 `.kpi-row` 横向排列，桌面端最多 4-5 列。
- 数字大、标签小，避免多余装饰。
- 书库 KPI 中「总书数」需随题材过滤实时变化。

### 管线（Pipeline）

- 横向阶段条，每个阶段包含图标、状态点、名称。
- 阶段最大宽度 160px，避免宽屏拉伸变形。
- 当前运行阶段使用脉冲动画与强调色。

### 侧边卡片

- 用于聚集次要信息（系统健康、最近任务、统计）。
- 标题 + 内容， hover 仅切换背景与边框色，无上浮阴影。
- 避免单张卡片过高导致空白。

### 抽屉 / 模态

- **报告详情**：右侧滑出抽屉，宽 420px，用于编辑核心洞察与详细分析。
- **设计编辑**：居中大窗模态，宽 1000px，类似写作页沉浸体验。
- 所有抽屉/模态带遮罩，点击遮罩或关闭按钮可关闭。

### 表单字段

- label 在上，input/textarea 在下。
- 深色背景、浅色边框，聚焦时边框变为强调色。
- textarea 支持纵向拉伸。

---

## 六、页面设计说明

### 工作台（Dashboard）

- 桌面端采用 `1fr 360px` 两栏：左侧主信息，右侧辅助信息。
- Hero 区：左书名/题材/操作，右 3 个关键统计卡片。
- KPI 区：4 个横向紧凑卡片。
- 管线区：横向展示末世管线 10 个阶段。
- 最近动态：填充主区域，避免底部空白。

### 书库（Library）

- 顶部题材标签横向滚动，点击过滤书籍。
- KPI：总书数、已入库、待处理、题材数（全局）。
- 总书数 / 已入库随当前题材实时更新。
- 书籍卡片展示封面、书名、作者、字数、题材标签。

### 拆书（Disassembly）

- 顶部 KPI：总任务、进行中、已完成、失败。
- 拆书管线可视化：文本导入 → 章节拆分 → 节奏分析 → 技法提炼。
- 任务卡片网格展示各任务进度。
- 右侧边栏：待拆书队列、最近完成、拆书统计。

### 报告（Reports）

- 顶部过滤标签：全部 / 写作指导 / 市场分析 / 人物分析 / 节奏拆解 / 技法提炼。
- 主区域 2 列报告卡片，显示标题、摘要、标签、元信息。
- 右侧边栏：报告统计、最近生成、待处理洞察。
- 点击卡片打开右侧详情抽屉，可编辑并获取 AI 建议。

### 设计（Design）

- 左侧垂直标签：粗纲 / 细纲 / 世界观 / 角色 / 势力。
- 各标签下使用卡片网格展示内容。
- 点击整卡进入编辑（不再使用编辑按钮）。
- 编辑界面为 1000px 居中大窗模态，支持 AI 建议与保存。

### 写作（Writing）

- 普通模式：顶部工具栏 + 标题输入 + 正文 textarea + 右侧辅助面板。
- 专注模式：Fullscreen API 真全屏，隐藏顶部/侧边/chrome，工具栏 sticky 置顶。
- 专注工具栏：退出专注 / 去AI味 / AI对比 / 字数 / 保存草稿。
- 去AI味：底部提示条显示检查项。
- AI对比：右侧滑出专用面板，窄屏自适应宽度。

### 硬件（Hardware）

- 仪表盘网格展示 GPU/VRAM/CPU/RAM 使用率与温度。
- 下方实时日志面板。
- 卡片最大宽度 220px，避免宽屏过度拉伸。

### 设置（Settings）

- 桌面端 3 列卡片网格。
- 每张卡片一个设置主题：主题、模型、写作偏好、快捷键、数据、账户。

---

## 七、响应式策略

| 断点 | 行为 |
|------|------|
| <= 768px | 侧边栏变抽屉；各页面转为单列；KPI 单列；报告卡片单列 |
| 769px - 1200px | 侧边栏 icon-only；dashboard/report 转单列 |
| >= 1201px | 完整侧边栏；dashboard 双栏；report 双栏；settings 3 列 |
| 1920px 全屏 | 所有内容按创作工作站密度排布，重点消除大面积空白 |

---

## 八、动效规范

- 过渡时长统一 0.15s - 0.25s，使用 ease。
- 卡片 hover：背景变为 `--surface-hover`、边框变为 `--border-hover`，无上浮/无营销化阴影。
- 统计卡片（`.kpi-card`）hover：在背景/边框变化基础上允许 `transform: translateY(-2px)`，提供轻微层级反馈，但不使用阴影扩散。
- 主按钮 hover：使用 `filter: brightness(1.08)`，无上浮。
- 运行中状态点使用脉冲动画。
- 抽屉/模态/下拉菜单使用 transform 滑入/缩放，避免重排。

---

## 九、截图归档

所有最终效果截图存放于 `prototype/screenshots/`，命名规则：`v{版本号}_{页面}[_{状态}].png`。

> 注：v7.13 起 dashboard 不再区分空状态截图，首次进入的空状态通过 localStorage 控制，截图以有项目状态为准。

v7.17 已归档：
- `v7.17_sky_dashboard.png`
- `v7.17_tomato_dashboard.png`
- `v7.17_tomato_writing.png`
- `v7.17_sky_hardware.png`

---

## 十、v7.8 更新记录

1. **桌面工作台重做**：从四列 Bento 改为「主区域 + 右侧边栏」创作者工作站布局；Hero 加大、管线横向阶段可视化、系统健康半圆仪表、写作进度大环形图。
2. **响应式边栏**：<=768px 抽屉；769-1100px icon-only 窄边栏；>=1101px 完整边栏。
3. **书库详情抽屉化**：窄屏下右侧面板改为滑出抽屉，带遮罩与关闭按钮。
4. **编辑器字体加大**：普通模式 18px/1.8 行高，专注模式 20px/2.0 行高。
5. **封面去重**：基于书名 hash 生成稳定渐变角度与色调，同题材内也有差异。
6. **题材标签横向滚动**：避免换行，支持触控/鼠标横向滚动。
7. **版本号统一升级至 v7.8**。

## 十一、v7.9 更新记录

1. **管线状态与题材绑定**：工作台 Hero 与管线标题明确显示「当前处理题材：末世」，管线卡片标题改为「末世管线」，让新人作者一眼知道当前激活的分析管线。
2. **布局稳定性修复**：补全缺失的 `--space-6` 变量；设置页桌面端固定 3 列避免过散；硬件仪表盘限制卡片最大宽度；管线阶段限制最大宽度，防止宽屏拉伸变形。
3. **报告页可编辑**：点击报告卡片滑出详情抽屉，支持编辑「核心洞察」「详细分析」，一键获取 AI 建议并保存。
4. **设计页可编辑**：卷、章、角色、势力、世界观卡片均加入「编辑」按钮，点击后弹出统一编辑抽屉，支持 AI 建议与保存后实时刷新。
5. **专注模式真全屏**：调用 Fullscreen API 进入全屏；顶部工具栏 sticky 置顶；「去AI味」显示底部提示条；「AI对比」在专注模式下从右侧滑出专用对比面板，窄屏下自适应宽度，关闭按钮始终可见。
6. **全量截图归档**：补充 dashboard / library / reports / report-detail / design / design-edit / settings / hardware / writing / focus / focus-ai 共 11 张 v7.9 最终截图。
7. **版本号统一升级至 v7.9**。

## 十二、v7.10 更新记录

1. **全屏（1920×1080）测试环境**：使用 Playwright 在 1920×1080 视口下批量截图，确保后续所有布局调整都基于宽屏创作工作站视角验证。
2. **工作台去空**：Hero 改为左右两栏紧凑布局，右侧统计卡片带背景；KPI 改为 4 个横向小卡片并配图标；主区域新增「最近动态」模块，避免主栏短于侧边栏导致的大面积空白。
3. **书库 KPI 精简**：移除「总字数」卡片；「总书数」「已入库」随题材切换实时更新为当前类型数量；「题材数」保持全局。
4. **拆书页面重填**：新增「拆书管线」可视化；主区域保留任务卡片；右侧边栏新增「待拆书队列」「最近完成」「拆书统计」三块内容，消除原页面大片空白。
5. **报告页面重做**：新增顶部过滤标签（全部 / 写作指导 / 市场分析 / 人物分析 / 节奏拆解 / 技法提炼）；报告卡片从 3 张扩充到 6 张，带标签与元信息；右侧新增「报告统计」「最近生成」「待处理洞察」汇总卡片。
6. **设计页交互重做**：移除各卡片上的「编辑」按钮，改为点击整卡进入编辑；编辑界面从右侧抽屉改为 1000px 居中大窗模态，类似写作页沉浸体验；卷、章、角色、势力、世界观核心卡片全部支持点击编辑。
7. **全量 v7.10 截图**：归档 dashboard / library / library_xianxia / disassembly / reports / design / design_modal / settings / hardware / writing 共 10 张 1920×1080 全屏截图。
8. **版本号统一升级至 v7.10**。

## 十三、v7.11 更新记录

1. **工作台空状态引导**：新增 `currentProject` 本地存储状态；首次无项目时工作台展示「开始你的第一部作品」空状态，提供「创建新作品」「导入参考书籍」「查看示例项目」三个清晰 CTA，并附带拆书/节奏/技法/AI 写作四大功能介绍。
2. **主题持久化**：主题选择写入 `localStorage`，刷新后保持所选主题。
3. **草稿持久化**：写作页标题与正文自动/手动保存到 `localStorage`，刷新后恢复。
4. **设置页可交互**：本地模型、API Endpoint、云端模型、数据目录、自动保存等输入项支持编辑与本地存储；新增「导出设置」「重置默认」按钮。
5. **书库搜索与导入**：书库顶部新增搜索框，支持按书名/作者/题材过滤；新增「导入书籍」按钮，随机导入示例书籍并刷新列表。
6. **跨页面状态隔离**：`navigate()` 在切换页面前统一关闭所有抽屉/模态/详情面板，避免模态污染。
7. **URL Hash 路由持久化**：页面切换时同步更新地址栏 hash，刷新或浏览器前进/后退可恢复当前页面。
8. **z-index 变量统一**：在 `:root` 中定义 `--z-base/--z-topbar/--z-sidebar/--z-overlay/--z-drawer/--z-sticky/--z-tooltip/--z-focus-panel/--z-toast/--z-modal`，替换全部硬编码 z-index，避免层级冲突。
9. **基础可访问性优化**：为移动端菜单按钮、导航项、关闭按钮、主题选择圆点添加 `aria-label`；为装饰性 SVG 添加 `aria-hidden`。
10. **全量 v7.11 截图**：归档 dashboard / dashboard_empty / library / library_xianxia / disassembly / reports / design / design_modal / settings / hardware / writing 共 11 张 1920×1080 全屏截图，并清理旧版 v7.10 截图。
11. **版本号统一升级至 v7.11**。

## 十四、v7.12 更新记录

1. **DS v4 Pro 多角色审视问题修复**：修复 P0 创建作品 `prompt()` 弹窗、拆书管线 4 阶段与后端 9 阶段不对齐、拆书→报告→写作数据闭环断裂、设计页产出无法在写作页消费、`coverStyle()` HSL 负值等 6 项严重问题。
2. **数据联动增强**：报告详情页新增「导入写作辅助」按钮，写作页新增「导入报告」与「大纲参考」面板，设计数据与报告数据可在写作场景消费。
3. **动态书库标签与 KPI**：题材标签从硬编码改为 `renderGenreTabs()` 动态渲染；KPI 卡片从硬编码改为按实际书籍数据计算。
4. **补充可访问性修复**：
   - 主题选择圆点支持 Enter/Space 键盘切换
   - 为按钮、导航项、输入框添加 `:focus-visible` 焦点环
   - 统一响应式断点为 `768px`，消除 CSS 与 JS 移动判断不一致
5. **状态与路由健壮性**：
   - 无效 URL hash 自动回退工作台并提示
   - `loadProject()` 对 `localStorage` JSON 增加非法数据校验与清理
6. **搜索与题材标签联动**：书库搜索时题材标签数量实时更新，搜索为空时提示「未找到含「xxx」的书籍」。
7. **HTML 结构验证**：补齐缺失的 `</div></body>` 闭合标签；`lint` 48/48 通过。
8. **新增辅助函数**：`escapeHtml()` 用于安全渲染用户可见文本，`renderOutlinePanel()` 修复写作页大纲渲染。
9. **全量 v7.12 截图**：归档 dashboard_empty / dashboard / library / library_search / disassembly / reports / design / settings / writing / invalid_hash 共 10 张 1920×1080 全屏截图，并清理旧版 v7.11 截图。
10. **版本号统一升级至 v7.12**。

## 十五、v7.13 更新记录

1. **严重编码损坏修复**：DS v4 Pro 拆分 `index.html` 后产生 211 处中文乱码（`�?`），已按上下文与源文件修复；同时修正 `第?00字` 等字面占位符。
2. **前后端数据打通修复**：
   - 修复 `restoreRoute` 缺少 `disassembly`，拆书页刷新不再跳回工作台
   - 修复报告页「导入写作辅助」误用旧 mock 数据（`REPORT_DATA`）而非后端动态数据（`_DYNAMIC_REPORTS`）
   - 修复报告卡片 meta 出现 `undefined`，根因是 `guidance.opening_hook_benchmark` 键名错误
   - 修复 `api_rhythm_plan` 插值键名（`hook_mean`/`conflict_mean`/`pleasure_mean`）
   - 修复深度诊断详情直接 `JSON.stringify` 展示，改为结构化文本
   - 修复指令面板参考书下拉为空，并增加缓存戳防止旧版 JS 缓存
3. **AI 骨架接入修复**：
   - 修正角色设定「预演未来 24 小时」为项目真实设定「4 小时」
   - 设计页左侧子导航新增「势力」tab，势力设定从世界观面板独立
4. **UI/UX 修复**：
   - 硬件栏高度回归 24px、topbar 回归 48px、侧边栏回归 210px，符合设计系统
   - 书库卡片移除误导性「已拆」badge
   - 创建作品弹窗点击内容区不再意外关闭
   - 章节号输入框 placeholder 从「加载中...」改为「章」
   - 移除 Google Fonts 外链，避免离线/网络受限环境阻塞
5. **安全与健壮性**：
   - 报告卡片、报告详情、书库详情、设计编辑、势力设定、逐章指令等渲染统一使用 `escapeHtml()`，防止 XSS 与属性破坏
   - `loadApiData()` 仅在至少一个端点成功时才标记 `ready=true`
   - `renderOutlinePanel()` 增加空卷纲保护
6. **配置 SSOT**：
   - `prototype/server.py` 端口、题材、数据目录、是否自动打开浏览器统一读取 `config.yaml` 中 `prototype` 段
   - 前端 `API_BASE` 改为相对路径，消除多处 `http://localhost:8080` 硬编码
7. **全量 v7.13 截图**：归档 dashboard / library / disassembly / reports / reports_detail / design / writing / settings / hardware / invalid_hash 共 10 张 1920×1080 全屏截图，并清理旧版 v7.12 截图。
8. **版本号统一升级至 v7.13**。

## 十六、v7.14 更新记录

1. **移除 data.js 硬编码**：前端改运行时异步加载 `library_data.json`，消除书库数据双副本。
2. **新建任务弹窗 checklist 动态渲染**：从 `library_data.json` 实时生成书籍选择列表，书库增减自动同步。
3. **importBook() 真实导入**：点击「导入书籍」唤起本地 TXT 文件选择器，解析文件名估算字数、猜测题材并加入书库。
4. **createTask() 真正提交后端**：
   - 前端 POST `/api/tasks` 创建任务
   - 后端启动后台线程调用 `xiaoshuo.pipeline.rhythm_analyzer` 处理选中的书籍
   - 前端轮询 `/api/task?id=...` 获取真实进度与状态
   - 任务列表持久化到 `localStorage`，刷新页面后可恢复未完成任务轮询
5. **api_skeleton 真正接入后端**：
   - `server.py` 引入 `outline_builder.load_guidance_benchmarks` 与 `generate_rhythm_plan`
   - 骨架的 300 章细纲均注入真实节奏目标（hook/conflict/pleasure target）
   - 卷纲节奏目标融合 `opening_hook_benchmark` 与 `dominant_conflict_types`
6. **设置页模型与 config.yaml 同步**：新增 `/api/config` 返回本地模型/云端模型，设置页自动填充。
7. **CSS z-index 归一**：响应式 sidebar 与 overlay 改用 `--z-sidebar` 变量，避免魔法数字。
8. **全量 v7.14 截图**：归档 dashboard / library / disassembly / reports / reports_detail / design / design_factions / writing / settings / hardware / task_modal 共 11 张 1920×1080 全屏截图，并清理旧版 v7.13 截图。
9. **版本号统一升级至 v7.14**。

## 十七、v7.15 更新记录（硬件监控 Redesign）

1. **专业方案**：将原本贯穿 topbar 的硬件条形图改为顶部状态徽标 + 下拉面板：
   - 正常时仅显示一个圆点 + 简短状态文字，不再挤占 topbar 横向空间。
   - 异常时徽标变为 warn/danger 色并伴随脉冲动画，高可见但不扩张。
   - 点击徽标展开 240px 下拉面板，展示 GPU/VRAM/CPU/RAM 四项实时指标与状态标签。
   - 面板底部保留「查看完整监控」入口，可跳转至独立硬件页面进行深度查看。
2. **状态机**：新增 `HW_THRESHOLDS` 阈值表与 `getHwStatus/getOverallHwStatus`，指标按 normal/warn/danger 分层，下拉面板标题、副标题、指标标签同步着色。
3. **设置项同步**：设置页「硬件监控中」改为「顶部显示硬件状态」，开关状态持久化到 `localStorage`，关闭后 topbar 徽标立即隐藏，完整硬件页面仍可独立访问。
4. **CSS 修复**：
   - 修复两处 `:root` 选择器误写成 `root` 导致的变量失效问题。
   - 补齐 `--border-hover`、`--shadow-lg`、`--z-popover` 等缺失变量。
   - 移除旧 `.hardware-bar` 相关样式，调整 `app-shell` grid 布局。
5. **全量 v7.15 截图**：归档 dashboard / dashboard_hw_open / settings / settings_hw_hidden 共 4 张关键截图。
6. **版本号统一升级至 v7.15**。

## 十八、v7.16 更新记录（UI 收敛与沉浸化）

1. **Topbar 精简**：
   - 移除 6 个彩色主题圆点，topbar 右侧仅保留一个主题图标按钮，循环切换 深色 / 浅色 / 高对比。
   - 模型 pill 默认隐藏，仅在本地模型端点无法连接或配置异常时显示红色警告 pill。
   - 新增 topbar 项目名显示，切换作品时同步更新。
   - 新增保存状态文本，编辑器输入时显示"保存中..."，防抖后显示"已保存"。
2. **导航选中态强化**：
   - 选中项左侧色条从 3px 加粗到 4px。
   - 选中项背景改为半透明品牌色背景 `rgba(var(--accent-rgb), 0.10)`。
   - 选中项图标同步使用品牌色 `var(--accent)`。
3. **写作页沉浸式编辑器**：
   - `.writing-layout` 默认 `sidebar-collapsed`，编辑器区域最大宽度 820px 居中，占据页面主体。
   - 右侧参考面板默认折叠为隐藏状态，通过悬浮 toggle 展开/收起。
   - 工具栏精简为左侧"专注模式" + "AI 辅助"下拉（去AI味 / AI 对比 / 逐章指令），右侧字数 + "保存草稿"。
   - "加载章节"等次要操作收到侧边栏内部或下拉菜单。
4. **仪表盘空状态去卡片化**：
   - 移除空状态外层深色卡片背景与边框，内容直接置于页面背景。
   - 主入口简化为"创建新作品"与"导入参考书籍"两个按钮。
   - "查看示例项目"降级为文字按钮。
   - 底部 4 个 feature 入口从 4 列等宽卡片改为 2 列无卡片简洁布局。
5. **主题系统收敛**：
   - 从 6 套主题收敛为 3 套：深色（midnight，默认）、浅色（light，新增）、高对比（obsidian）。
   - 删除 ocean / forest / ember / nebula 主题 CSS 与入口。
   - 新增 light 主题变量，适配亮色背景与深色文字。
   - 旧版 localStorage 中保存的已删除主题自动 fallback 到 midnight。
6. **模型状态检测**：新增 `checkModelStatus()`，启动时及每 30 秒检测一次本地模型连通性。
7. **全量 v7.16 截图**：归档 dashboard / writing / settings / settings_light 共 4 张关键截图。
8. **版本号统一升级至 v7.16**。

## 十九、v7.17 更新记录（设计语言研究 + 品牌色预设）

1. **设计语言研究**：以 Datadog（暗色 + 高密度数据）/ Linear（极简 + 紫色 + Inter）/ 番茄作家助手（沉浸 + 护眼）为参照，提炼通用设计语言；经对比后仅保留最符合项目气质的配色方向，探索文档已清理。
2. **主题（surface）与品牌色（accent）解耦**：
   - 主题保留 v7.16 的 midnight / light / obsidian 三套，仅控制背景层与文字色。
   - 品牌色最终保留两套预设：默认天蓝 `#38BDF8`（minimax 修改前原默认色）/ 方案 C 番茄 `#F86840`，仅控制 `--accent` 与 `--accent-rgb`。
   - 两套可正交组合，理论上 3 × 2 = 6 种视觉变体。
3. **SSOT 落地**：在 `config.yaml` 的 `prototype.theme_presets` 段声明品牌色，`server.py /api/config` 透传给前端；前端保留同构 fallback，确保离线/配置缺失时行为一致。
4. **Topbar 切换器**：主题图标旁保留"切换品牌色"图标按钮，点击循环天蓝 / 番茄两套预设并 toast 当前名称。
5. **设置页面板**：新增"品牌色（强调色）"设置卡片，与"主题"卡并列；按钮动态从 `theme_presets` 渲染。
6. **Light 主题适配**：在 light 主题下，两种品牌色自动切换到对应的深色变体（`#0ea5e9` / `#e05a36`），保证亮底文字对比。
7. **Brand 标记变化**：`.brand-logo` 背景色绑定 `--accent`，品牌色切换时 logo 同步过渡（200ms）。
8. **全量 v7.17 截图**：归档 `v7.17_sky_dashboard.png` / `v7.17_tomato_dashboard.png` / `v7.17_tomato_writing.png` / `v7.17_sky_hardware.png` 共 4 张关键对比图。
9. **版本号统一升级至 v7.17**.

## 二十、v7.18 更新记录（UI 细节打磨）

1. **顶部状态 pill 语义化**：硬件/模型状态 pill 使用实心语义色（成功绿/警告黄/危险红），避免橙色主题下警告文字不可见。
2. **书库 KPI 精简**：移除冗余的「已入库」重复卡片，统一为「总书籍 / 已拆章节 / 待处理 / 题材数」。
3. **书籍封面按题材着色**：新增 `GENRE_COLORS` 映射，末世深红、仙侠墨绿、科幻深蓝等，提升扫描效率。
4. **拆书空状态填充**：无任务时显示最近完成案例 + 新建任务入口，避免页面空洞。
5. **管线流程可视化**：pipeline-stage 之间增加连接线，已完成段使用主题色半透明连线，强化流程感。
6. **筛选标签填充态**：genre-tab / filter-tab / report-filter 选中时使用实心主题色背景，提高选中辨识度。
7. **统计数字语义色**：拆书侧边栏关键数值使用 success/warning/danger 语义类。
8. **项目卡片去渐变**：dashboard hero-station 改为纯色面 + 顶部主题色细线，降低视觉噪音。
9. **版本号统一升级至 v7.18**.

## 二十一、v7.19 更新记录（借鉴言悦AI流程可视化）

1. **工作台「创作阶段」可视化条**：将 9 步技术管线抽象为作者视角 4 阶段（①准备素材 → ②分析爆款 → ③获取指导 → ④手写正文），用 completed / running / pending 状态区分，hover 显示阶段详情，底部文案显示当前阶段与进行中的任务。
2. **拆书「快速开始向导」**：任务为空时不再显示简单的「暂无运行中任务」，而是展示 3 步向导（选择题材 / 导入书籍 / 启动管线）+ 新建任务按钮 + 示例《全球高武》按钮，降低新用户认知负担。
3. **全局「继续写作」悬浮按钮**：右下角固定 FAB，显示笔尖图标 + 「继续写作」+ 当前章节名，点击直达写作页；移动端自动隐藏章节名节省空间。
4. **书库「题材热度排行」**：在书籍网格顶部增加横向热度条，按书籍数量排序，点击可快速筛选对应题材，让作者一眼知道哪个题材数据最充分。
5. **修复全局 FAB 遮挡底部内容**：为 `.main` 增加 `padding-bottom: calc(var(--space-5) + 72px)`，确保滚动到页面最底部时内容不会被右下角「继续写作」悬浮按钮遮挡。
6. **缓存戳升级至 v7.22**：配合 `server.py` 的 `Cache-Control: no-store` 头部，减少浏览器缓存旧样式导致显示异常的概率。
7. **版本号统一升级至 v7.19**.

## 二十二、v7.19 第二轮打磨（针对言悦借鉴审查反馈）

1. **警告标签彻底脱钩主题色**：`model-status-pill.status-offline` 固定为 `#EAB308`（黄底黑字），`status-error` 固定为 `#DC2626`（红底白字），避免在番茄/琥珀主题下与主题色混淆导致不可见。
2. **管线连接线状态语义化**：已完成阶段之间使用绿色实线；从 running 阶段出发使用主题色虚线；pending 阶段之间使用灰色虚线，强化流程状态的可读性。
3. **题材热度条形图改为真实比例**：条形宽度按 `count / total` 计算，末世 32/52 = 61.5%，hover 提示显示精确百分比。
4. **同一题材内书籍色块增加明度区分**：`GENRE_PALETTES` 为每个题材配置 3 个同色相不同明度的颜色，根据书名哈希选择，同一行内同题材书籍不再完全相同。
5. **创作阶段与技术管线层级说明**：管线标题从「末世管线」改为「技术管线详情」，并增加副标题「当前阶段：获取指导 · 第 7/9 步 · 末世题材」，明确区分作者视角 4 阶段与技术视角 9 步管线。
6. **全局悬浮写作按钮 hover 反馈增强**：`transform: translateY(-3px) scale(1.05)` + 更强阴影 + 图标旋转放大。
7. **书库「导入书籍」按钮降级为次级按钮**：从 `btn-primary` 改为 `btn-secondary`。
8. **缓存戳升级至 v7.23**.

## 二十三、v7.20 书库重构（列表/网格混合视图）

1. **书库默认改为列表视图**：类似 IDE 文件树/Notion 数据库，一行一本书，列包含书名、作者、题材、字数、节奏评分、技法卡数、状态。
2. **表头支持排序**：点击列标题可在升序/降序之间切换，当前排序列高亮显示箭头。
3. **题材标识降级**：列表视图用 8px 彩色圆点 + 文字；网格视图用左侧 3px 细线，不再使用左侧大色块。
4. **新增状态/评分/技法卡维度**：
   - 状态：已拆书/分析中/待分析/失败（图标 + 彩色胶囊标签）
   - 节奏评分：≥8 绿色、6-8 黄色、<6 红色
   - 技法卡数：根据字数估算
5. **网格视图去色块**：保留网格布局，但去掉高饱和左侧大色块，改为低饱和首字大写占位 + 左侧细线 + 边框标签。
6. **视图切换按钮**：书库筛选栏右侧增加列表/网格切换按钮，默认列表，用户可随时切换。
7. **缓存戳升级至 v7.24**.

## 二十四、v7.20 第二轮打磨（针对书库/工作台/拆书审查反馈）

1. **书库字数列右对齐**：`.col-wordCount` / `.col-cards` 改为 `text-align: right`，便于数字大小比较。
2. **书库默认排序改为节奏评分降序**：`librarySort` 初始值从 `{title, asc}` 改为 `{score, desc}`，让用户第一眼看到高分书籍。
3. **工作台技术管线可折叠**：`pipeline-header` 增加点击折叠/展开，默认展开，箭头旋转反馈，避免两个横向流程条同时占用视觉焦点。
4. **创作阶段条与技术管线间距拉开**：`.creative-stage-bar` 增加 `margin-bottom: var(--space-5)`。
5. **拆书引导页与管线步骤互斥显示**：无任务时只显示 `quick-start-wizard` 并隐藏拆书管线；有任务时显示管线并隐藏引导页。
6. **拆书引导页步骤状态反馈**：根据当前状态动态标记 3 步为 completed/running/pending，已完成步骤显示对勾，当前步骤主题色高亮。
7. **统计卡片 hover 反馈增强**：`.kpi-card` / `.library-kpi-card` hover 时 `translateY(-3px)` + 更强阴影。
8. **书库批量选择**：列表视图行首增加 checkbox，支持单选/全选，选中行高亮，底部显示批量操作栏（已选 X 本 + 批量拆书）。
9. **缓存戳升级至 v7.25**.

## 二十五、v7.21 逻辑一致性打磨（针对审查反馈）

1. **书库"待处理"统计对齐**：KPI 卡片"待处理"改为统计 `status` 为空 / `imported` / `pending` 的书籍数量，与列表状态一致。
2. **拆书引导页与筛选互斥**：`renderTasks` 在 `taskFilter !== 'all'` 时显示"暂无xx任务"空状态，不再显示引导页；只有"全部"且无任务时才显示向导。
3. **拆书队列有书时引导文案**：如果 `tasks.length > 0` 但当前筛选无匹配任务，引导页标题从"开始你的第一次拆书分析"改为"继续拆书分析"，副标题显示队列数量。
4. **书库题材标识去重**：书名前小圆点移到"题材"列，作为题材列图标前缀，避免与题材文字重复。
5. **批量操作栏增强**：操作栏改为 sticky 底部 + 左侧主题色边线 + 阴影，选中后始终可见。
6. **工作台管线图标按钮组**："跨书合成运行中"右侧的下拉箭头改为图标按钮组（暂停 / 停止 / 详情 / 折叠），并带 tooltip。
7. **全局时间戳精确化**：新增 `timeAgo()` 函数，今天内显示"X小时/分钟前"，昨天显示"昨天 HH:MM"，当年更早显示"MM-DD HH:MM"，跨年显示"YYYY-MM-DD HH:MM"。
 8. **缓存戳升级至 v7.26**.

## 二十六、v7.21 报告/设计页打磨（针对审查反馈）

1. **报告页卡片描述限制 2 行 + hover 展开**：标题加粗放大，描述默认 2 行截断，hover 卡片时展开完整内容。
2. **报告"重新生成"下沉到卡片级别**：每个卡片右上角增加刷新图标（hover 显示），顶部按钮改为"批量重新生成"。
3. **设计-世界观结构化**：从纯文本段落改为"世界观核心摘要 + 时间线/核心空间/规则系统/关键势力"四维度卡片。
4. **设计-粗纲卷进度指示**：为每卷增加进度条和"已写 X / Y 章"文本，未开始卷显示"未开始"。
5. **设计-角色隐藏角色处理**：人物关系图中保留"变异体零号"节点，角色卡片区默认隐藏，增加"展开全部角色"按钮。
6. **设计-角色关系图点击高亮**：点击节点高亮当前节点 + 相关连线 + 关联节点，其余元素变暗。
7. **设计-势力卡片 hover 反馈 + 编辑图标**：卡片 hover 上浮 + 边框高亮，hover 时显示编辑图标按钮。
8. **设计-粗纲"从后端加载骨架"按钮文案**：改为"从 AI 生成骨架"，并增加 tooltip 说明。
9. **缓存戳升级至 v7.27**.

## 二十七、v7.28 细纲重构与设计页整体打磨

1. **细纲改 10 章一段（章组级）**：从单章卡片改为 `segment-card`，包含段标题、段目标、核心冲突、段结果、10 章迷你节拍、进度条、情绪/冲突强度迷你折线、标签、承上启下。
2. **章组迷你节拍状态**：每章用一个小方块表示，`written` 绿、`outlined` 琥珀、`blank` 灰，hover 显示章标题。
3. **标签分类色系统**：新增 `.tag-type-plot`（情节-蓝）、`.tag-type-character`（角色-紫）、`.tag-type-emotion`（情绪-琥珀）、`.tag-type-location`（地点-绿）、`.tag-type-item`（物品-粉）。
4. **粗纲卡片等高对齐**：`.volume-card` 使用 flex 布局，`summary` 撑满，tags 始终贴底；进度条按完成度显示语义色（zero 灰/low 红/mid 琥珀/high 绿）。
5. **世界观页填充**：核心摘要 + 4 维度卡片（规则/势力高亮）+ 末日时间轴 + 势力关系预览图。
6. **编辑弹窗层级增强**：顶部主题色条 + 更强阴影 + 抬升感；增加「取消」按钮；AI 建议改为次级 outline，保存为主按钮；textarea 最小高度提升到 160px。
7. **设计页右侧状态面板**：显示当前状态（总章节/已写作/规划中/进度）、最近修改、AI 待办。
8. **子导航图标化**：粗纲/细纲/世界观/角色/势力均增加 SVG 图标，hover 微移反馈。
9. **卡片 hover 反馈增强**：细纲/粗纲/世界观维度卡片 hover 时上浮 + 阴影 + 边框高亮。
10. **缓存戳升级至 v7.28**.

## 二十八、v7.29-v7.30 写作页/硬件页/设置页重构

### v7.29
1. **去AI味提示条最终修复**：`.deai-hint` 默认 `display:none`，仅在写作页且开启时显示，避免非写作页露出头部文字。

### v7.30 写作页重构
2. **三栏布局**：左 `writing-toc` 章节目录 + 卷高亮；中 `editor-pane` 拉宽至自适应；右 `writing-sidebar` 本章细纲/角色速查/设定速查/大纲参考/AI 对比/逐章指令。
3. **顶部状态栏重排**：左侧卷级上下文 + 章节导航 + 全书进度条；中间专注模式 + AI 辅助下拉；右侧字数/保存脉冲点/保存草稿。
4. **AI 辅助菜单图标化**：图标+文字+快捷键，"去AI味"改为"AI指纹检测"。
5. **编辑器增强**：标题与正文层级分离（标题 28px 加粗 + 下划线分隔），段落行高 1.9，选中字数统计，底部字数/目标显示。
6. **专注模式沉浸化**：隐藏 topbar/sidebar/工具栏/继续写作按钮，仅保留中央编辑器 + 底部半透明统计，显示 `focus-hint`。

### v7.30 硬件页重构
7. **双模型状态卡片**：主模型 Qwen3.5-9B（运行中）+ 交叉模型 DeepSeek-R1-0528（待机），展示 TTFT/速度/VRAM。
8. **统一指标卡片**：GPU 温度/VRAM/RAM/CPU/功耗，均带趋势迷你图和语义色。
9. **VRAM 分配详情**：分段柱状图展示主模型/系统/KV Cache/空闲。
10. **任务队列**：进行中/等待任务列表 + 进度条。
11. **日志增强**：OK/WARN/ERROR 级别着色 + 过滤按钮。

### v7.30 设置页重构
12. **双模型配置**：主模型 + 交叉模型端点（8002）。
13. **生成参数**：ctx、Temperature、Top-p、KV Cache 量化、切换策略（VRAM 阈值 swap_to）。
14. **编辑器设置**：字体大小、行高、段落间距、自动保存间隔、目标字数、打字机模式开关。
15. **管线设置扩展**：拆书并行度、S3 评审严格度、缓存保留时长。
16. **品牌色实时预览**：标签+按钮+进度条预览。
17. **数据管理**：数据库大小、缓存清理、备份、导出配置、重置偏好。
18. **快捷键扩展**：上一章/下一章、AI指纹检测、AI辅助面板、自检面板。

### 全局
19. **底部空白填充**：`.main` 设 `min-height: 100vh`，`.page` 高度自然延展。
20. **缓存戳升级至 v7.30**.

## 二十九、v7.31-v7.33 写作页深度打磨（信息密度与定位优化）

### v7.31 右侧标签切换
1. **右侧面板从 7 个堆叠面板改为 4 个标签**：`细纲` / `角色` / `设定` / `AI工具`，单屏内切换，避免垂直滚动 7 次。
2. **标签语义色区分**：`AI工具` 标签激活时使用品牌色背景；`AI指纹检测清单` 中检测出的问题项使用琥珀色边框/背景（`panel-warning` / `check-item-warning`）。
3. **内容区占满剩余高度**：`.sidebar-tab-content` 使用 `flex:1` + `overflow:auto`，每个标签内部独立滚动。
4. **缓存戳升级至 v7.31**.

### v7.32 左侧面板定位与目录结构
5. **当前章节统一为 127 章**：顶部上下文、面包屑、目录、大纲参考、逐章指令输入框统一显示第 127 章 / 第三卷。
6. **目录动态生成 300 章**：根据 5 卷 × 60 章 结构渲染全部章节，当前卷展开，其他卷收起。
7. **当前章吸顶 + 自动滚动**：`.toc-chapter.current` 使用 `position:sticky;top:0`，目录加载后自动 `scrollIntoView` 到可视区顶部。
8. **最近修改快捷区**：显示当前章、上一章、上周同期章（127 / 126 / 120），一键跳转。
9. **大纲参考内容校正**：根据当前章 127 动态计算为 `第三卷：暗流涌动` + `第 121-130 章` 段目标。
10. **缓存戳升级至 v7.32**.

### v7.33 编辑器细节与 AI 对比高亮
11. **顶部进度条语义明确化**：`本章 15 / 2000 字 · 1%` + 6px 迷你进度条，避免横线长度歧义。
12. **底部状态栏信息补全**：左（本段字数 / 选中字数）、中（预计剩余时间 / 今日字数）、右（目标字数）。
13. **标题与正文分隔增强**：`.editor-title` 保留底部 2px 边框 + 标题下方间距，形成清晰层级。
14. **AI 对比差异高亮**：使用 `.diff-del`（红色删除线）和 `.diff-ins`（绿色下划线/背景）标注原文与 AI 润色的差异。
15. **继续写作按钮避让**：右侧面板底部留 `padding-bottom:64px`，FAB 不再遮挡面板内容。
16. **缓存戳升级至 v7.33**.

## 三十、v7.34 场景参考面板

### 新增功能
1. **写作页 AI 工具标签内新增「场景参考」面板**：位于「逐章指令」面板之后，通过后端 `/api/search` 检索 33 本书库中的相似场景段落。
2. **搜索交互**：输入框支持回车搜索，占位示例为「怎么写拍卖场冲突…」，右侧提供搜索按钮。
3. **结果卡片**：展示相似度百分比、书名、章节号、情绪/节奏/冲突/爽点标签、技法摘要，以及可折叠的原文预览。
4. **联调提示**：后端未启动时显示「连接失败，请确认后端已启动(python -m xiaoshuo.api.server --port 8089)」。
5. **样式一致性**：使用 `panel-accent` 品牌色边框，输入框/标签/卡片均复用现有 CSS 变量。

### 附：v7.31-v7.33 CSS 样式抢救
6. 因 git checkout 误操作丢失了 v7.31-v7.33 未提交的 CSS，已在本版本中一并恢复：左侧目录、标签切换、编辑器头尾、进度条、diff 高亮、顶部工具栏上下文等关键样式。
7. **缓存戳升级至 v7.34**.

## 三十一、v7.35 拆书页与书库统计集成

### 新增 API 消费
1. **书库卡片增加拆书统计微标签**：在 `renderLibraryList()` 和 `renderLibraryGrid()` 中，若 `book.disassembly` 存在，展示「章节数 / 主导情绪占比 / 快节奏占比」标签。
2. **拆书页接真实数据**：页面加载时调用 `/api/disassembly/books`，渲染 4 个 KPI（已拆书籍 / 总章节 / 总字数 / 平均章字数）、右侧「拆书列表」和「拆书统计」。
3. **拆书详情弹窗**：点击列表书名打开模态框，调用 `/api/disassembly/book?name=...` 展示基础信息、情绪/节奏/冲突/爽点分布条形图、前 30 章逐章数据。

### 实现细节
  4. 书名使用 `encodeURIComponent()` 编码，避免中文特殊字符问题。
  5. 静态服务器下 API 不可用时，KPI 显示 0，列表显示「加载失败，请确认后端已启动」。
  6. **缓存戳升级至 v7.35**.

## 三十二、v7.35 UX 全局审视与修复

### 发现的问题
1. **版本号不一致**：`<title>` 为 v7.19、顶部 `version-badge` 为 v7.17，与实际 v7.35 不符。
2. **硬件页布局崩溃**：v7.30 重构后的硬件页 CSS 丢失，导致模型卡片、指标卡片、VRAM 分配、任务队列、运行日志等区域错位，底部出现巨大橙色斜线。
3. **全局底部提示污染**：每个页面底部都显示「按 Esc 或移动鼠标到顶部呼出工具栏」，在普通浏览模式下无意义。
4. **系统状态矛盾**：后端未启动时，顶部模型状态 pill 显示红色「配置获取失败」，与绿色「系统正常」形成强烈反差。
5. **继续写作 FAB 章节不一致**：显示「第 128 章《突围》」，与写作页当前第 127 章不统一。
6. **设置页标签断行**：「模型名」「端点」等短标签被浏览器断成单字换行。
7. **拆书页旧 mock 元素残留**：过滤标签（全部/进行中/已完成/失败）和「正在处理 · 3/9」的拆书管线均为假数据，与真实 API 逻辑冲突。

### 修复内容
8. **统一版本号**：`<title>` 与 `version-badge` 同步为 v7.35。
9. **恢复硬件页 CSS**：重写 `.model-status-grid`、`.model-status-card`、`.metric-card`、`.vram-breakdown-card`、`.task-queue-card`、`.log-panel` 等样式，恢复 v7.30 的卡片 + 日志布局。
10. **底部提示仅在专注模式显示**：`.focus-hint` 默认隐藏，`body.focus-mode` 下才显示，并改为固定定位药丸样式。
11. **模型状态降级为黄色**：配置获取失败时显示「本地模型未启用」+ warn 黄色，避免误报错误。
12. **统一继续写作 FAB**：改为「第 127 章《暴雨前的寂静》」。
13. **设置页标签防断行**：`.settings-row > span` 与 `.settings-row-stack > span` 增加 `white-space: nowrap`。
14. **拆书页清理旧 mock**：移除过滤标签与假管线；中间区域改为「拆书数据区」空状态提示，引导用户从右侧列表打开详情。

## 三十三（补）v7.35-v7.36 抢救丢失的书库 CSS

### 问题
15. 因 `git checkout` 误操作导致 v7.31-v7.33 的书库列表/网格视图 CSS 丢失，书库页在列表视图下所有书籍信息垂直堆叠、表头与内容错位。

### 修复
16. 在 `styles.css` 中恢复 `.library-content`、`.book-list-header`、`.book-list-row`、`.book-list-col`、`.book-list-checkbox`、`.book-list-score`、`.book-list-status`、`.book-list-batch-bar`、`.grid-card`、`.grid-cover-block`、`.cover-abbr`、`.tag-outline`、`.genre-rank-*`、`.filter-tabs` 等关键样式。
17. 列表视图恢复为 8 列网格（选择/书名/作者/题材/字数/节奏评分/技法卡/状态），拆书微标签在书名下方内联显示。
18. 网格视图恢复为自动填充卡片布局，封面块、书名、作者、字数、评分、拆书微标签分层展示。

## 三十三、v7.36 写作页「风格校准」面板

### 新增功能
1. 在写作页右侧面板 **AI 工具** 标签内新增「风格校准」panel，位于「AI 对比分析」与「逐章指令」之间。
2. **状态显示**：展示当前累积的风格规则数量，空状态时提示「暂无规则，完成 S3 评审后自动积累」。
3. **操作按钮**：「校准当前章」调用 `POST /api/style/calibrate`，「刷新」调用 `GET /api/style/rules`。
4. **规则列表**：以 `[dimension] rule xweight` 形式展示规则，列表最大高度 200px，超出滚动。
5. **降级提示**：后端 API 未就绪时，按钮操作后显示「后端未连接（风格校准需要 API server）」。

### 实现要点
6. JS 新增 `calibrateStyle()` / `loadStyleRules()` / `renderStyleRules()` 三个函数，使用 `API_BASE` 统一前缀。
7. 复用现有 `.panel-accent` 与 `.btn-secondary` 样式，保持 AI 工具标签视觉一致性。
8. **缓存戳升级至 v7.36**.

## 三十四、v7.37 间距语言修复：工作台 + 写作页 + 设计页

### 问题 1：工作台「你的创作阶段」显示错乱
1. 因 CSS 丢失，`creative-stage-bar` 内的步骤垂直堆叠，无进度条样式。
2. 修复：新增 `.creative-stage-bar`、`.stage-bar-track`、`.stage-bar-step`、`.stage-bar-connector`、`.stage-bar-dot`、`.stage-bar-info`、`.stage-bar-status` 等完整样式，恢复水平步骤条 + 状态连线 + 当前状态高亮。
3. 同步将第 4 步 tooltip 从「第 128 章《突围》」改为「第 127 章《暴雨前的寂静》」。

### 问题 2：写作页三栏间距不统一
4. `.main` 内边距从 `var(--space-5)`（24px）缩到 `var(--space-4)`（16px），让所有页面主内容区紧贴左侧导航。
5. `.writing-layout` 从 `flex + max-width:1100px + margin:auto` 改为 **CSS Grid**：
   ```css
   grid-template-columns: 200px 1fr 260px;
   gap: var(--space-4);
   ```
   目录、编辑器、辅助面板三栏间距统一由 `gap` 控制，消除左右留白不均。
6. `.writing-sidebar` 宽度从 280px 缩到 260px，内部 panel 间距从 12px 缩到 10px。
7. `.outline-section` / `.ai-compare-item` 的 `margin-bottom` 从 12px 缩到 10px，右侧面板更紧凑。

### 问题 3：设计页左侧空白
8. 同样受益于 `.main` padding 缩小，设计页 `.subnav` 与全局侧边导航之间的空白从 24px 减到 16px。

### 验证
9. 工作台阶段条、写作页三栏、设计页布局均已截图验证。
10. **缓存戳升级至 v7.37**.

## 三十五、v7.38 书库网格卡片标签显化

### 问题
11. 书库网格视图中的书籍标签使用了 `.tag-outline`（透明背景 + 灰色边框），在深色主题下几乎不可见，看起来像「标签没了」。

### 修复
12. `renderLibraryGrid()` 中，网格卡片的题材/标签改回 `.tag` 样式（带品牌色背景），并优先读取 `DATA.tagPools[b.title]`，最多显示前 3 个标签。
13. 如果一本书没有额外标签池，则保底显示题材标签。
14. **缓存戳 / 版本号升级至 v7.38**.

### 补充：v7.38 书库列表视图标签 + 章节列
15. 用户反馈列表视图中「书籍标签」依旧不可见，且部分书未显示章节数。
16. 修复：
    - 列表视图表头新增「章节」列（第 6 列），有拆书数据时显示 `disassembly.chapters`，无数据时显示 `-`。
    - 书名下方新增 `.book-list-tags` 区域，显示该书标签池前 2 个标签（无标签池时保底显示题材）。
    - 未拆书的书籍在书名下方显示「未拆书」提示，避免空白。
    - 修正 `book-list-header` / `book-list-row` 的 Grid 列数为 9 列，防止状态列被挤出。

## 九、截图归档规范